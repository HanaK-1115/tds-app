import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e293b',
    },
    secondary: {
      main: '#f50057',
    },
  },
  // その他のテーマ設定...
});

export default theme;